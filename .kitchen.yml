---
driver:
  name: vagrant
provisioner:
  name: chef_zero

# Test all of the platforms that we support for this cookbook.
platforms:
  - name: ubuntu-14.04
  - name: ubuntu-12.04
  - name: centos-7.1
  - name: centos-6.6

suites:
  - name: headnode
    run_list:
      - 'recipe[blp-bcpc::headnode]'
  - name: worknode
    run_list:
      - 'recipe[blp-bcpc::worknode]'
